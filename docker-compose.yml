version: "3.3"
services:
  exloli:
    image: "hpilosit/exloli-cat:latest"
    container_name: exloli
    volumes:
      - ./:/app
    logging:
      options:
        max-size: "128m"
    restart: unless-stopped
